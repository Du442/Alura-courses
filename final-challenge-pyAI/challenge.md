Final Challenge(Using an IDE):

1 - Add a .txt file, where each line will be a Python element.

2 - Send it to the model you are running locally to extract, in json, where each item will have an item "user", "original_review", "review_pt", "rating" (positive, neutral, negative)

3 - Transform an model answer in a PYTHON dictionary´s list.

4 - Create an function what, given the dictionary´s list, travel the list and make 2 things:

a. count the quantity of positive, neutral and negative reviews.
b. unite each item of this list in a string type variable with anyone separator.

at the end, return both things.
